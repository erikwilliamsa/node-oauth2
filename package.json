{
  "name": "node-oauth-example",
  "version": "1.0.0",
  "description": "example of oauth2 with node",
  "main": "index.js",
  "scripts": {
    "start": "node main.js",
    "test": "$(npm bin)/mocha --reporter nyan",
    "watch-test": "$(npm bin)/mocha --recursive  -w config/**/*.js -w app/**/*.js --R nyan tests/",
    "build": "$(npm bin)/webpack",
    "buildStart": "npm run build && npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.5.2",
    "express": "^4.14.0",
    "express-session": "^1.14.1",
    "express-sessions": "^1.0.6",
    "passport": "^0.3.2",
    "passport-facebook": "^2.1.1"
  },
  "repository": "https://github.com/erikwilliamsa/oauth2_examples",
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.2",
    "request": "^2.75.0",
    "request-promise": "^4.1.1",
    "sinon": "^1.17.6"
  }
}
